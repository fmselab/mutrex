package regex.distinguishing;

/**
 * The Enum DSgenPolicy.
 */
public enum DSgenPolicy {

	/** The random. */
	RANDOM,
	/** The pref positive. */
	PREF_POSITIVE,
	/** The pref negative. */
	PREF_NEGATIVE,
	/** The strictly positive. */
	STRICTLY_POSITIVE,
	/** The strictly negative. */
	STRICTLY_NEGATIVE,
	/** The only positive. */
	ONLY_POSITIVE,
	/** The only negative. */
	ONLY_NEGATIVE
}